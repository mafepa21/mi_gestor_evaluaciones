# P2 Control Strip

Objective: add a lighter reusable macOS control surface and apply small transitions to status/loading affordances.

Files:
- `kmp/iosApp/MacApp/MacPremiumComponents.swift`

Do:
- Keep existing `MacPremiumFilterBar`.
- Add a lower-chrome alternative for dense screens.
- Add subtle transitions to small overlays only.

Expected output:
- `MacPremiumControlStrip`.
- Small loading/status transitions.
