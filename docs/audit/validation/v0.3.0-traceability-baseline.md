# Validación — v0.3.0-traceability-baseline

Este documento registra los resultados reales de la validación técnica e integración para la línea de corte `v0.3.0-traceability-baseline`. 

## Estado de la Validación

| Plataforma | Build | Prueba funcional | Resultado | Fecha | Motivo / Evidencia |
|---|---|---|---|---|---|
| iOS | Xcode / esquema | Cuaderno, rúbricas y asistencia | **Pendiente** | — | Entorno local sin Xcode completo. El comando `xcodebuild` falló al apuntar a `/Library/Developer/CommandLineTools`. |
| macOS | Xcode / esquema | Planner y shortcuts | **Pendiente** | — | Entorno local sin Xcode completo. El comando `xcodebuild` falló al apuntar a `/Library/Developer/CommandLineTools`. |
| Android | Gradle | Arranque y persistencia | **Pendiente** | — | Entorno local sin SDK de Android instalado (falta el directorio `/Users/mariofernandez/Library/Android/sdk`). |
| Flutter | flutter test/analyze | Base Drift y fórmulas | **Pendiente** | — | Flutter SDK / FVM no disponible en la ruta local del sistema. |

## Resumen del Entorno de Ejecución

- **Xcode Command Line Tools**: Activado (`/Library/Developer/CommandLineTools`), pero no dispone de la suite de Xcode completa necesaria para compilar el proyecto SwiftUI.
- **Android SDK**: No configurado en la máquina del agente.
- **Flutter SDK**: No instalado en la máquina del agente.

> [!IMPORTANT]
> Esta baseline actúa como referencia histórica y técnica de la consolidación de ramas del 6 de julio de 2026. Al no haberse podido completar una regresión funcional o compilación limpia automatizada debido a las limitaciones del entorno local, la versión **no es considerada una release estable distribuible** y requiere validación en un equipo con el SDK completo antes de su paso a producción.

## Verificación de Integridad de la Baseline (Git)

Las siguientes comprobaciones fueron ejecutadas en local el 10 de julio de 2026 para garantizar la integridad e inmutabilidad de la baseline:

```bash
$ git fetch --tags origin
$ git cat-file -t v0.3.0-traceability-baseline
tag

$ git show --no-patch --format=fuller v0.3.0-traceability-baseline
tag v0.3.0-traceability-baseline
Tagger:     marfepa <mario.fernandez@scorazon.hhdc.net>
TaggerDate: Fri Jul 10 09:33:03 2026 +0200

Baseline de trazabilidad tras integración masiva del 2026-07-06

commit 9c067a14604e556a249e7352ff0918f34dc574f0
Author:     marfepa <mario.fernandez@scorazon.hhdc.net>
AuthorDate: Mon Jul 6 21:07:05 2026 +0200
Commit:     marfepa <mario.fernandez@scorazon.hhdc.net>
CommitDate: Mon Jul 6 21:07:05 2026 +0200

    docs: update development memory with git reorganization

$ git merge-base --is-ancestor 9c067a14 origin/main && echo "OK: la baseline sigue siendo ancestro de main"
OK: la baseline sigue siendo ancestro de main

$ git log --first-parent --decorate --oneline v0.3.0-traceability-baseline..origin/main
abcaa9ed (HEAD -> main, origin/main, origin/HEAD) docs(audit): establish v0.3.0 traceability baseline audit logs and future workflow governance
```

Conclusiones del chequeo:
- El tag es de tipo anotado (`tag`), guardando autor, fecha y mensaje.
- El tag apunta exactamente al commit `9c067a14` correspondiente a la consolidación de la integración masiva.
- El commit del tag es un ancestro lineal directo del `main` actual, sin manipulaciones ni force-push detectados.

