# Task Plan

1. Fix course structure duplication so rubric-linked columns preserve their evaluation/rubric association in the destination course.
2. Add course structure duplication entry point to the iOS notebook view.
3. Add copy/paste evaluation actions to the iOS bulk rubric evaluation screen.
4. Verify the shared Kotlin build and the iOS app build.
5. Prevent notebook work-group creation from reusing an existing group's name in the same tab, so new groups do not silently replace old ones.
6. Improve notebook column interactions on desktop and iOS so columns can be reordered by drag-and-drop, resized from a corner, and recolored from a long-press menu while keeping cell widths aligned with headers.
